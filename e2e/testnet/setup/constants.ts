import { baseSepolia } from "viem/chains";

export const rpcHttpUrl = 'https://sepolia.base.org';
export const chain = baseSepolia

export const extraAccountsFilePath = './extra-accounts.json';
export const extraAccountsNumber = 10;