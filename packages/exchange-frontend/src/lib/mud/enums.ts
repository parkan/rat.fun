export enum ENVIRONMENT {
  UNKNOWN = "unknown",
  DEVELOPMENT = "development",
  BASE_SEPOLIA = "base-sepolia",
  BASE = "base"
}

export enum WALLET_TYPE {
  UNKNOWN = "unknown",
  BURNER = "burner",
  ENTRYKIT = "entrykit"
}
