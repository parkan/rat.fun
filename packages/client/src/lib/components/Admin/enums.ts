export enum TRIP_EVENT_TYPE {
  BASELINE = "trip_baseline",
  VISIT = "trip_visit",
  DEATH = "trip_death",
  CREATION = "trip_created",
  LIQUIDATION = "trip_liquidated",
  DEPLETED = "trip_depleted"
}

export enum VALUE_CHANGE_DIRECTION {
  POSITIVE = "positive",
  NEGATIVE = "negative",
  NEUTRAL = "neutral"
}
