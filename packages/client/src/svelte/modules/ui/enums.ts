export enum UI {
  LOADING,
  SPAWNING,
  READY,
  ERROR,
}

export enum LOCATION {
  NONE,
  MAIN,
}

export enum PANE {
  NONE,
  LEFT,
  MIDDLE,
  RIGHT,
}

export enum LEFT_PANE {
  YOUR_RAT,
  YOUR_ROOMS,
  SINGLE_ROOM,
  CREATE_ROOM,
}

export enum RIGHT_PANE {
  ROOMS,
  SINGLE_ROOM,
  ROOM_RESULT,
}
