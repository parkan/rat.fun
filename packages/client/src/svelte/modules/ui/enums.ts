export enum UI {
  LOADING,
  SPAWNING,
  CREATING_RAT,
  READY,
  ERROR,
}

export enum LOCATION {
  NONE,
  MAIN,
}

export enum PANE {
  LEFT,
  MIDDLE,
  RIGHT,
}

export enum LEFT_PANE {
  YOUR_RAT,
  YOUR_ROOMS,
}

export enum RIGHT_PANE {
  ROOMS,
  SINGLE_ROOM,
}
